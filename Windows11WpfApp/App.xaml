<Application x:Class="Windows11WpfApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Windows11WpfApp"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Windows 11 样式资源 -->
        <ResourceDictionary>
            <!-- 颜色定义 -->
            <SolidColorBrush x:Key="BackgroundColor" Color="#1F1F1F" />
            <SolidColorBrush x:Key="ControlBackgroundColor" Color="#2D2D2D" />
            <SolidColorBrush x:Key="AccentColor" Color="#0078D4" />
            <SolidColorBrush x:Key="TextColor" Color="#FFFFFF" />
            <SolidColorBrush x:Key="BorderColor" Color="#404040" />
            <SolidColorBrush x:Key="HoverBorderColor" Color="#606060" />
            
            <!-- 圆角定义 -->
            <CornerRadius x:Key="DefaultCornerRadius">8</CornerRadius>
            
            <!-- 窗口样式 -->
            <Style TargetType="Window">
                <Setter Property="Background" Value="{StaticResource BackgroundColor}" />
                <Setter Property="Foreground" Value="{StaticResource TextColor}" />
                <Setter Property="WindowStyle" Value="None" />
                <Setter Property="AllowsTransparency" Value="True" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="UseLayoutRounding" Value="True" />
            </Style>
            
            <!-- 容器样式 -->
            <Style TargetType="Grid">
                <Setter Property="Background" Value="{StaticResource ControlBackgroundColor}" />
            </Style>
            
            <!-- 按钮样式 -->
            <Style TargetType="Button">
                <Setter Property="Background" Value="{StaticResource ControlBackgroundColor}" />
                <Setter Property="Foreground" Value="{StaticResource TextColor}" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="{StaticResource DefaultCornerRadius}" />
                <Setter Property="Padding" Value="8,4" />
                <Setter Property="Margin" Value="8" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="UseLayoutRounding" Value="True" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="BorderBrush" Value="{StaticResource HoverBorderColor}" />
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="{StaticResource HoverBorderColor}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <!-- 滑块样式 -->
            <Style TargetType="Slider">
                <Setter Property="Width" Value="200" />
                <Setter Property="Height" Value="40" />
                <Setter Property="Margin" Value="8" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="UseLayoutRounding" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Slider">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Border Grid.Row="1" Background="{StaticResource BorderColor}" Height="4" CornerRadius="2" />
                                <Track Grid.Row="1" Name="PART_Track">
                                    <Track.DecreaseRepeatButton>
                    <RepeatButton Background="{StaticResource AccentColor}" Height="4" />
                </Track.DecreaseRepeatButton>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Background="Transparent" Height="4" />
                </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb Width="20" Height="20" Background="{StaticResource ControlBackgroundColor}" BorderBrush="{StaticResource AccentColor}" BorderThickness="2" CornerRadius="10">
                                            <Thumb.Style>
                                                <Style TargetType="Thumb">
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource AccentColor}" />
                                                            <Setter Property="BorderBrush" Value="{StaticResource TextColor}" />
                                                        </Trigger>
                                                        <Trigger Property="IsDragging" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource AccentColor}" />
                                                            <Setter Property="BorderBrush" Value="{StaticResource TextColor}" />
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Thumb.Style>
                                        </Thumb>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>